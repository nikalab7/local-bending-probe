# Cached PDB downloads — regenerated by the scripts, do NOT commit (hundreds of MB)
t4l_pdb/
cull_pdb/
val_pdb/

# Python
__pycache__/
*.pyc

# Keep the figures (*.png) and the .md docs — they belong in the portfolio.
